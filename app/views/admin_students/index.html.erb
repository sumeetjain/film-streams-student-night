<div class="container">
	<div class="container bottom_space">
	  	<div class="row">
	    	<div class="col-md-6 col-md-offset-3">
	    		<h1 class="text-center">Manage Students</h1>
	      		<h2 class="text-center">Search by name (or partial name).</h2>
	      		<div>
	   				<%= form_for "findingStudents", method: 'get' do |f| %>
		      			<div class= "form-group">
			      			<%= f.label "name" %>
			      	   		<%= f.text_field "name", class: "form-control"%>
			      	   		<%= f.submit "Search", class: "btn custom-btn col-xs-4 col-xs-offset-4" %>
		      			</div>
	      			<% end %>
      			</div>
	  		</div>
		</div>
	</div>
</div>

